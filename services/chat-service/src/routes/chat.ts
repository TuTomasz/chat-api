import { FastifyInstance } from "fastify";
import { z } from "zod";

export const chat = async (fastify: FastifyInstance) => {
  // TODO VIEW MESSAGES BETWEEN TWO USERS
  // TODO SEND MESSAGE TO A USER
};
