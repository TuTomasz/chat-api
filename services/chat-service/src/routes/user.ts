import fastify, { FastifyInstance } from "fastify";
import { z } from "zod";

export const user = async (fastify: FastifyInstance) => {
  // TODO USER SIGNUP
  // TODO USER LOGIN
  // TODO GET ALL USERS EXCEPT THE CURRENT USER
};
